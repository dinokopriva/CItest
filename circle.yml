deployment:
  staging:
    branch: master
    commands: 
      - docker login --email=dinokoprivnjak@axilis.com --username=dinokoprivnjak@axilis.com --password=5316bb80-4e42-4cf2-9e2b-6972bc528877 registry.heroku.com
      - docker push ci_test:latest

machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build --rm=false -t ci_test:latest . -f ./dockerfile
